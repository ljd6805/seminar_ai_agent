# AI Agent 세미나 — 페이지별 구성 (v0.11)

> 각 페이지가 "무엇을 담는가" 수준의 분해. 시각 표현·디자인은 추후.
> **1시간 30분(약 90분) 기준. 총 40장 = 발표 38장 + 부록 2장(넘기지 않음).**
> **데모는 라이브가 아니라 미리 만든 예시를 자료에 삽입.**
> 데모 4개: 데모 1(도입 훅) · 데모 2(skill) · 데모 3(MCP) · 데모 4(harness 유무). **데모 2·데모 4는 하나의 "문서 parsing" 작업을 공유.**
> 데모 라벨: 청중 노출은 **데모 1~4**, 내부 관리번호는 Ex1(데모 1)·Ex2(데모 2)·Ex5(데모 3, v0.11 신규)·Ex4(데모 4). Ex3=GitOps는 제외된 결번 — 청중에게 노출하지 않음.

---

## 도입 (약 26분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 1 | 표지 | 제목 "AI Agent, 오늘부터 쓸 수 있습니다" · copy "무엇인지 알고 → 왜 좋은지 보고 → 오늘 바로 써보기까지" |
| 2 | 목차 | 5부 여정(도입/본문1/본문2/본문3/마무리) — "낯선 단어는 나오는 자리에서 전부 풀어서 + 부록 용어집" 안심 문구 |
| 3 | AI의 발전 (역사) | 7노드 타임라인: 2012 AlexNet · 2017 Transformer · 2020 GPT-3 · 2022 ChatGPT · 2023 function calling · 2024 MCP · 2024–25 agent loop. lead가 두 시대 구분("앞의 넷이 말하는 AI를, 뒤의 셋이 일하는 AI를"), note가 "불과 2~3년"(왜 지금 복선) + LLM 기초 브리지 |
| 4 | LLM 기초 (3분 기초) | 오늘 쓸 개념 셋 — LLM=다음 말 예측(계산 아닌 예측→틀릴 수 있음) · 컨텍스트=유한한 작업 기억(넣어준 것만 안다) · 토큰=사용량 단위. "확장 요소들이 전부 이 한계를 다루는 장치" 복선 |
| 5 | **데모 1 (Ex1)** ⭐ | **regression 로그 실패 분류 장면** (정적) — 좌 대화 6버블(수집→유형 제안→대표 케이스→원인 불명은 사람이), 우 "실패 분류 리포트" 5행. **정의보다 앞 = show→tell.** "구성된 예시 장면" 라벨 명시 |
| 6 | 데모 1 해부 | 방금 화면의 세 장면 → 핵심 3요소 매핑: 표 정리 제안=다단계 · 리포트 성장=도구 사용 · "대표 케이스 붙여줘" 반영=결과 확인 |
| 7 | AI agent란? | "방금 보신 것처럼—" 데모 콜백 정의. 기존 vs agent 2칸 대조 + 요약 띠 "도구 사용 · 다단계 진행 · 결과 확인" |
| 8 | 챗봇 vs agent | 같은 LLM, 차이는 도구+루프 — 챗봇(질문→답변, 말로 끝) vs agent(목표→계획·실행·확인↻→작업물). flow-lane 대조 |
| 9 | 왜 지금 | '지금'의 근거(도구 호출 2023·연결 표준 2024 + 사내 LLM 보유) → 시간 재배분 대조. 브리지: "우리 일과 무슨 상관? 설계·검증의 하루를 봅니다" |
| 10 | 왜 우리 — 설계·검증의 반복 | 도메인 pain 3칸: 스펙·문서 / 로그·리포트(원인 줄 찾기·regression 분류) / 코드·스크립트. "규칙은 분명한데 손이 많이 가는 일 = agent가 가장 잘하는 유형" |
| 11 | 어떻게 — 일의 재배치 | 기존(전 과정 사람) vs 요구되는(정의→agent 실행→확인·판단) flow 대조. 톤=기술(記述), 척추 A 유지. 브리지: "어떤 일부터 맡기면 될까?" |
| 12 | 무엇을 맡기나 | 위임 기준 — 맡기기 좋은 일 vs 아직 사람. 기준 한 줄 "결과를 내가 바로 확인할 수 있는가". 민감 데이터 → 본문 1 브리지 |

## 본문 1 — 우리 환경 = 정보 경계 (약 12분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 13 | 간지 | "우리 환경 — 데이터는 어디로 가는가" |
| 14 | 격리 다이어그램 | 사용자 노드 두 갈래 분기 — 위(초록) 사내 AI agent(OpenCode·Roo·DS assistant API), 아래(경고색) 빗금 경계 벽 관통 → 사외 AI agent(Claude, 도입 예정). **보안 원칙 유일한 온전 진술** — 이후 장은 콜백만 |
| 15 | 사내 도구 셋 | OpenCode(터미널 클라이언트) · Roo(VS Code agent) · DS assistant API(직접 호출 API). 접속 안내 placeholder(`______`) |
| 16 | 보내도 되는 것 · 안 되는 것 | 판단 사례 — 사내 경로: 업무 자료 활용(권한 경계는 유지) / 사외 경로: RTL·스펙·미공개 정보 금지. **"정확한 기준은 팀 보안 규정이 우선" 명시** |

## 본문 2 — 확장 요소: skill · MCP (약 27분) — v0.11: plugin 제외, 요소별 "개념→구조·동작→데모" 배열

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 17 | 간지 | "확장 요소 — agent에게 능력을 더하는 법" (skill · MCP 두 단어) |
| 18 | 맨몸 모델의 한계 | 문제 제기 — **두 구멍**: 지식(우리 절차 모름) · 연결(사내 시스템에 못 닿음). "절차는 skill, 연결은 MCP — 하나씩, 데모와 함께" |
| 19 | skill 개념 | 절차를 파일(SKILL.md)로. 없으면(매번 다시 설명) vs 있으면(한 번 적고 계속) 대조. 비유 "잘 쓰는 사람의 매뉴얼을 팀 서랍에" |
| 20 | skill 구조·동작 | 파일트리(SKILL.md+참조) + progressive disclosure ①이름·설명 상주 ②맞으면 본문 로드 ③참조는 그때만 |
| 21 | 데모 2a — 사람의 일 → SKILL.md | 상단 **강조 박스**: "① 메모를 읽고 → ② 결정·할 일·기한을 골라내 → ③ 정해진 형식으로 옮겨 적기" / 하단 좌 **작성법 박스**(이름·설명 → 절차 번호 목록 → 형식 규칙, SKILL.md 부위와 1:1) / 하단 우 SKILL.md 실물 |
| 22 | **데모 2b (Ex2) — 실행** | 상단 flow: 비정형 메모 → **meeting-notes skill(세 단계 대신 수행)** → 표준 JSON. 입력/출력 터미널 대조(JSON은 선언 스키마와 완전 일치). note = 달라진 것 3가지 + 도메인 다리 |
| 23 | MCP 개념 | 개방형 표준, "AI용 USB-C". 없으면(N×N 제각각) vs 있으면(한 번 만들어 어디서나) 대조. OpenAI·Google도 채택 |
| 24 | MCP 구조·동작 | client–server + tools·resources·prompts + ①연결·목록 교환 ②골라 호출 ③결과 수신. note = 데모 브리지("다음 장에서 눈으로") |
| 25 | **데모 3 (Ex5) — MCP 연결** | (v0.11 신설, 구성된 예시 라벨) 이슈 트래커 MCP 서버 연결 장면 — 좌: 대화+tool 호출 로그(issue_search → 6건 수신), 우: 상태별 정리 결과. note = "로그인→검색→복사→붙여넣기가 사라짐 · skill=어떻게, MCP=어디까지" |
| 26 | 도구 지도 + 설치·주의 | 3칸 지도(Context 확장/웹·브라우저/Token 절약)×예 1개 + 설치 한 줄(전체 목록은 부록) + 주의 한 줄(일부 MCP 서버·도구는 외부 연결 → 보안 콜백) |

## 본문 3 — harness (약 18분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 27 | 간지 | "harness — 믿고 쓸 수 있게 만드는 틀" |
| 28 | 실패의 현실 (정직한 전제) | agent도 틀린다 — 실패①그럴듯한 오답(환각) · 실패②들쭉날쭉한 형식. "확인 자체를 틀에 넣을 수 없을까? → harness" |
| 29 | harness란? | 순서·규칙+검증+재시도의 실행 틀. verification harness와 같은 구조. **공식 근거**: Claude Code 문서의 "agentic harness" |
| 30 | UVM 나란히 | 두 패널 병렬 다이어그램 — sequence→DUT→assertion vs 입력·절차(skill)→모델→스키마 검증. "구조가 같고 대상만 칩에서 모델로" |
| 31 | 스키마 게이트 | 입력→skill→스키마 검증→통과/실패(한도 내 재시도 ↻). 스키마=assertion처럼 형식 게이트 + 정직한 한계(내용 정합은 사람이) |
| 32 | 장점과 단점 | 품질·재현성(동일 구조) vs 초기 설계·유지보수 비용. 브리지: "언제 치를 가치가 있나?" |
| 33 | 언제 쓰나 | 기준 한 줄 "출력이 사람에게 가면 맨 agent, 프로그램에게 가면 harness" |
| 34 | **데모 4 (Ex4)** ⭐ | 같은 작업 harness 유무 3회 대조 — 정점. bad run은 실제 깨진 출력 스냅샷. 축=품질+재현성("같은 구조" — 값 결정론 아님) |

## 마무리 (약 12분) + 부록

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 35 | 오늘 지나온 길 (회고) | 3칸 요약: 어디서(데이터는 사내에)·무엇으로(확장 요소 **둘**)·어떻게(틀로 감싸서) + fragment 펀치라인 |
| 36 | FAQ 선제 대응 | Q1 일이 없어지나 · Q2 어디까지 믿나 · Q3 뭘 공부하나 |
| 37 | 그래서, 어떻게 시작하나 (CTA) | 딱 하나: 사내 LLM에서 · 작은 작업 · 사람이 최종 확인 + **첫 프롬프트 실물 터미널** |
| 38 | Q&A 클로징 | A 착지+B 지평선. 지지대 칩(이 자료·부록·동료) |
| 39 | 부록 — 도구 목록 (넘기지 않음) | 확장 요소 후보 전체 목록 — 부록 B의 슬라이드 실체화 |
| 40 | 부록 — 용어집 (넘기지 않음) | 오늘 나온 용어 10개 한 줄 정의(LLM·컨텍스트·토큰·agent·환각 / skill·MCP·MCP 서버·harness·스키마) |

## 페이지 수 요약

- 도입 12 + 본문1 4 + 본문2 10 + 본문3 8 + 마무리 4 + 부록 2 = **40장** (발표 38 + 부록 2)
- fragment 3곳: 34장(harness 유무 대조) · 35장(회고 펀치라인) · 38장(B 지평선+지지대)

## 이 리스트에서 아직 열린 것

- 표지 발표자·일시, 15장 접속 안내(`______`), 26장 대표 도구 확정(현 claude-mem/playwright/yagni는 예시 placeholder)
- 디자인 세부 조정(개별 장 연출·간격) — 큰 컨셉은 확정(reveal.js + vibe 테마)

## v0.11 변경 이력 (2026-07-07) — plugin 제외 + 요소별 데모 배치

> 사용자 결정: 본문 2를 skill과 MCP만으로 구성, 각 요소 뒤에 해당 데모 배치.

- **plugin 장·관계 정리 장 삭제** — 맨몸 한계 3구멍→2구멍(지식·연결), 회고 "확장 요소 셋→둘", 용어집에서 plugin·hook 제거, 우산 용어 "확장 요소" = skill·MCP로 축소.
- **배열 변경**: skill 개념→구조·동작→데모 2(2장) → MCP 개념→구조·동작→**데모 3(신설)** → 도구 지도.
- **데모 3 (Ex5) 신설 — MCP 연결 장면**: 이슈 트래커 tool 호출 로그 + 정리 결과 (구성된 예시 라벨). skill=어떻게 / MCP=어디까지 관계 진술을 이 장이 흡수.
- harness 데모는 **데모 4**로 재라벨. 총 41→40장.
- (동일 버전대 초기 작업) 데모 1 애니메이션 제거·데모 2a 강조 박스 재구성·역사 병합(42→41)·"월요일" 표현 제거는 v0.9~v0.10 이력 참조.

## v0.10 변경 이력 (2026-07-07)

- **역사 Ⅰ·Ⅱ 1장 병합** (42→41장), 칩 "여기까지 온 길" → "도입 · 역사", 제목 "AI는 어떻게 agent가 되었나", 타임라인 점·선 정렬 교정.
- **데모 1 애니메이션 제거**: 정적 표시로 전환, 오토플레이 JS·재생 버튼 삭제.

## v0.9 변경 이력 (2026-07-07) — "발표자료 제작" 예시 폐기

- **데모 1 소재 교체**: 발표자료 제작 대화 → regression 로그 실패 분류 장면. 회고 장을 "오늘 지나온 길" 요약으로 교체. "월요일" 수사 표현 전면 제거.

## v0.8 변경 이력 (2026-07-07) — 20장 → 42장 전면 확장

> 발표 시간 60→90분 확정. 비판 분석(서사 구멍 A / 압축 과적 B / 도메인 공명 C) 전면 반영 — "많이 만들고 줄이기" 전략.

- A 서사: 맨몸 한계 · 실패의 현실 · LLM 기초 · SKILL.md 실물 · CTA 첫 프롬프트 / B 해체: skill·MCP 각 2장 / C 도메인: 설계·검증 pain · UVM 병렬 · 사내 도구+판단 사례 / 구조 장치: 목차·간지·챗봇 대조·해부·위임·언제·FAQ·용어집

## v0.5~v0.7 변경 이력 (2026-07-06~07, 요약)

- v0.7: "일의 재배치" 신설, 우리 환경 격리 다이어그램화 / v0.6: skill·MCP·plugin 각 1장 + reveal.js 재구축 / v0.5: 척추=A 확정, show→tell, 표기 규칙, Ex2 JSON 완결, harness 공식 용어 근거 / v0.4: 역사 6노드 검증, UVM 비유 채택, 성능 수치 미사용

---

## 부록 A — 데모 2 / 데모 4 공유 parsing 예시 (실제 내용)

> 하나의 "문서 parsing" 작업을 데모 2(skill)·데모 4(harness)가 공유.
> 아래는 **구성된 예시(illustrative)** — 실제로 참인 메커니즘(스키마 검증 + 재시도가 일관성·품질을 높임)을
> 보여주기 위한 대표 사례. 특정 시스템의 측정값이 아님.
> 원하면 사용자가 자신의 harness 실제 실행 결과로 교체 가능(선택).

### 공유 입력 문서 (회의 메모)

```
[회의 메모]
10/24 주간 동기화. 참석: 지훈, 민아, 세영.
- 로그인 API 응답 지연 이슈, 원인은 토큰 갱신 로직으로 확인. 지훈이 금요일까지 수정.
- 신규 대시보드 디자인 시안 3안 중 2안으로 결정. 민아가 다음 주 월요일까지 반영.
- 다음 회의 10/31 오후 3시.
```

- 중립적·누구나 이해 가능. 설계/검증 특화(UVM·RTL) 아님 → 범용 소개에 적합. (교체 가능)
- 슬라이드 rule에서 도메인 다리 반 줄로 연결("설계·검증에선 스펙 문서·검증 로그가 이 자리에").

### 사용 skill (21장에서 실물 공개)

```
meeting-notes/SKILL.md — name·description frontmatter + 절차 5줄
(제목·날짜·참석자 추출 / decisions / action_items{owner,task,due} / 날짜 YYYY-MM-DD / next_meeting)
```

### 목표 스키마

```
{ title, date, participants[], decisions[], action_items[{owner, task, due}], next_meeting }
```

### 데모 2 — 기본 동작 (22장)

비정형 문서를 skill이 한 번에 구조화. **잘 되는 케이스만** 보여줌. (스키마 완전 일치 — decisions·task 포함, due는 ISO로 통일. 2024-10-24=목요일 기준 금요일=10-25, 다음 주 월요일=10-28 — 날짜 검증됨)

```json
{
  "title": "주간 동기화",
  "date": "2024-10-24",
  "participants": ["지훈", "민아", "세영"],
  "decisions": ["대시보드 시안 2안으로 결정"],
  "action_items": [
    { "owner": "지훈", "task": "토큰 갱신 수정", "due": "2024-10-25" },
    { "owner": "민아", "task": "대시보드 2안 반영", "due": "2024-10-28" }
  ],
  "next_meeting": "2024-10-31 15:00"
}
```

### 데모 3 — MCP 연결 (25장, Ex5)

이슈 트래커 MCP 서버를 연결해 둔 상태에서 "이번 주 내가 맡은 이슈, 상태별로 정리해줘" → agent가 issue_search tool 호출 → 6건 수신 → 상태별 표(진행 중 2 · 리뷰 대기 3 · 막힘 1). **구성된 예시** — 이슈 ID(UI-142 등)는 가상. 가르치는 점: 연결(조회·로그인·복사)이 사람 손에서 사라짐.

### 데모 4 — 같은 작업, harness 유무 (34장, Ex4)

같은 입력을 **3번씩** 돌린다고 상상. 축은 **품질 + 재현성**. (슬라이드에도 아래 스냅샷을 실제 텍스트로 노출)

**harness 없이 (단발 호출, 실행마다 출력이 달라짐)**

- Run 1 — 필드 누락 + 구조 드리프트: `participants`가 배열이 아니라 한 문자열, `action_items`에 `due` 빠짐
  ```json
  { "title": "주간 동기화", "participants": "지훈, 민아, 세영",
    "action_items": ["로그인 API 수정", "대시보드 반영"] }
  ```
- Run 2 — 키 이름이 바뀜(스키마 불일치): 한글 키로 나옴
  ```json
  { "제목": "주간 동기화", "참석자": ["지훈","민아","세영"], "결정": ["2안 채택"] }
  ```
- Run 3 — JSON 깨짐(기계 파싱 불가): 설명 문장이 앞에 붙고 마지막 쉼표 오류
  ```
  다음은 회의 내용을 정리한 결과입니다:
  { "title": "주간 동기화", "date": "2024-10-24", }
  ```

→ **품질**: 누락·형식 오류 발생. **재현성**: 세 번 모두 형태가 다름 → 뒷단에서 받아쓸 수 없음.

**harness로 감싸면 (스키마 검증 + 불합격 시 재생성)**

- Run 1·2·3 — **매번 스키마에 일치**하는 완결 JSON(데모 2와 같은 구조).
  ※ 보장되는 것은 '스키마 일치(구조)'이지 값까지의 결정론이 아님 — 슬라이드도 "같은 구조"로 표기.

### 이 대조가 정직한 이유

- LLM 단발 호출의 스키마 미준수(키 누락·드리프트)와 JSON 앞 설명문(preamble)은 **벤더 공식 문서가 인정하는 실제 현상** — 지어낸 결함이 아님.
- harness의 "스키마 정의 → 검증 → 실패 시 재시도"는 출력을 스키마에 수렴시키는 실제 표준 기법.
- 그래서 위 예시는 **참인 현상을 대표 사례로 보여주는 것**. 단, 특정 harness의 측정 수치인 척은 하지 않음.
- 속도/퍼센트 같은 성능 숫자는 쓰지 않음 — 품질·재현성으로만 승부.

---

## 부록 B — 유용한 것들 전체 목록 (슬라이드 39장의 원천)

> 이 목록이 **슬라이드 39장(부록)**으로 실체화됨 — 발표 땐 넘기지 않고, 자료 배포 후 자가학습 진입로.
> 슬라이드 26장은 아래 3칸 중 **각 예 1개씩만** 본문에 노출.

| 카테고리 | 후보 | 본문 예시(26장) |
|----------|------|------------------|
| Context 확장 | ralph loop · claude-mem · superpowers | claude-mem (placeholder) |
| 웹·브라우저 | firecrawl · frontend design · playwright | playwright (placeholder) |
| Token 절약 | caveman · ponytail · yagni | yagni (placeholder) |

- 설치 안내는 슬라이드 26장에서 공통 한 줄("설정에 항목 하나 등록하는 수준").
- 대표(예시) 확정은 실제 사내/사외 환경 검증 후 — 현재는 placeholder.
