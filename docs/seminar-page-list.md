# AI Agent 세미나 — 페이지별 구성 (v0.6)

> 각 페이지가 "무엇을 담는가" 수준의 분해. 시각 표현·디자인은 추후.
> 1시간(약 60분) 기준. **총 19장 = 발표 18장 + 부록 1장(넘기지 않음).**
> **데모는 라이브가 아니라 미리 만든 예시를 자료에 삽입.**
> **데모 2·데모 3은 하나의 "문서 parsing" 작업을 공유** — 데모 2에서 기본 동작, 데모 3에서 같은 작업의 harness 유무 대조.
> 데모 라벨: 청중 노출은 **데모 1/2/3**, 내부 관리번호는 Ex1/Ex2/Ex4 (Ex3=GitOps는 제외된 결번 — 청중에게 노출하지 않음).

---

## 도입 (약 10분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 1 | 표지 | 제목 "AI Agent, 오늘부터 쓸 수 있습니다" · copy "무엇인지 알고 → 왜 좋은지 보고 → 오늘 바로 써보기까지" · flow "도입 → 사내/사외 AI agent 구성 → 확장 요소 → harness → 결론" |
| 2 | AI의 역사 | 6개 마일스톤 타임라인 — 강조는 '무엇이 가능해졌나', 용어(self-attention·few-shot·RLHF)는 괄호 보조. 2012 AlexNet · 2017 Transformer · 2020 GPT-3 · 2022 ChatGPT · 2023 function calling · 2024–25 MCP·agent |
| 3 | **데모 1 (Ex1)** ⭐ | *이 발표자료를 만든 대화 과정* 애니메이션 (자동재생, 대화+구조 성장). **정의보다 앞 = show→tell.** 얼개 라벨 "확장 요소 — skill·MCP·plugin" |
| 4 | AI agent란? | "방금 보신 것처럼—" 데모 콜백 정의. 기존 vs agent **2칸 대조** + 하단 요약 띠 "도구 사용 · 다단계 진행 · 결과 확인" |
| 5 | 왜 지금 우리가 | '지금'의 근거(도구 호출 2023·연결 표준 2024 + 사내 LLM 보유) → 시간 재배분 대조 → 본문 1 브리지 |

## 본문 1 — 우리 환경 = 정보 경계 (약 6분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 6 | 우리 환경 — 정보는 어디로 가나 | (구 6+7 병합) 경계 다이어그램: 사내에 머문다(사내 서빙 LLM — API 호출·OpenCode 클라이언트) vs 경계를 넘는다(사외 Claude Code, 도입 예정). **보안 원칙 유일한 온전 진술** — 이후 장은 콜백만 |

## 본문 2 — 확장 요소: skill · MCP · plugin (약 20분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 7 | **skill** (확장 요소 ①) | 의미=모델이 불러 쓰는 전문 절차 · 목적=반복 노하우를 한 번 적으면 같은 절차를 그대로 · 구조=폴더 하나(SKILL.md+참조 자료, 파일트리 터미널) · 동작=필요할 때만 펼치기 ①이름·설명 상주 ②맞으면 본문 로드 ③참조는 그때만(공식 비유 "목차→챕터→부록") |
| 8 | **MCP** (확장 요소 ②) | 의미=외부 연결 개방형 표준(Model Context Protocol, "AI용 USB-C") · 목적=제각각이던 연결의 표준화 · 구조=client–server, 서버가 tools·resources·prompts 노출 · 동작=①연결·목록 교환 ②골라 호출(로컬·원격) ③결과 수신. OpenAI·Google도 채택 |
| 9 | **plugin** (확장 요소 ③) | 의미=skill·MCP·hook을 묶은 배포 단위 · 목적=조합을 한 번에 설치·버전관리·공유 · 구조=plugin.json+skills/hooks/+.mcp.json(파일트리) + marketplace 설치·plugin명:이름 꼬리표 + 관계(꾸러미 vs 담기는 것) |
| 10 | **데모 2 (Ex2)** | 회의 메모 → 스키마 JSON. **JSON은 선언 스키마와 완전 일치**(decisions·task 포함, due ISO 통일). rule에 도메인 다리: "설계·검증에선 스펙 문서·검증 로그가 이 자리에" |
| 11 | 유용한 것들 + 설치·주의 | 3칸 지도(Context 확장/웹·브라우저/Token 절약)×예 1개 + 설치 한 줄(전체 목록은 부록) + 주의 한 줄(외부 연결 → 보안 콜백) + harness 브리지 |

## 본문 3 — harness (약 13분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 12 | harness란? | 확장 요소를 순서·규칙+검증+재시도로 감싸는 실행 틀. verification harness(UVM)와 같은 구조(DUT=모델). **공식 근거**: Claude Code 문서의 "agentic harness" |
| 13 | 스키마 게이트 | 입력→skill→스키마 검증→통과(출력)/실패(한도 내 재시도 ↻). 스키마=**assertion처럼 형식 게이트** + 정직한 한계(내용 정합은 별도 — 지금은 사람이) |
| 14 | 장점과 단점 | 품질(검증 통과분만 출력)·재현성(동일 구조) vs 초기 설계·유지보수 비용 |
| 15 | **데모 3 (Ex4)** ⭐ | 같은 작업 harness 유무 3회 대조 — 클라이맥스. 우측에 메커니즘 라벨 "(스키마 검증 → 불합격 시 재생성)". 축=품질+재현성("같은 구조" — 값 결정론 아님) |

## 마무리 (약 9분) + 부록

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 16 | 다시 그 장면 (정서 정점) | 데모 1 정지컷(재생 X). 3버블은 데모 1과 **글자 그대로 동일**. lead 한 문장: "처음엔 마법처럼 보이던 이 화면, 이제는 메커니즘으로 보이시죠." |
| 17 | 그래서, 월요일 아침 (CTA) | 딱 하나: 사내 LLM에서 · 스스로 판단 가능한 작은 작업(스펙 구조화·로그 요약) · 사람이 최종 확인하는 루프(harness는 도입되면). "애매하면 보안 먼저" 콜백 |
| 18 | Q&A 클로징 | A 착지+B 지평선: "원리를 아는 여러분 … 작은 작업 하나로 첫 대화를 // 그 하나가 쌓이면, 일하는 방식은 어느새 바뀌어 있을 겁니다." 지지대 칩(이 자료·부록·동료) |
| 19 | 부록 (넘기지 않음) | 확장 요소 후보 전체 목록 — 아래 부록 B의 슬라이드 실체화. 발표 시간 비용 0, 자가학습 진입로 |

## 페이지 수 요약

- 도입 5 + 본문1 1 + 본문2 5 + 본문3 4 + 마무리 3 + 부록 1 = **19장**
- 병합(구 6+7, 구 11+12)으로 발표 장수 2장 감소 — 데모 3개의 시간 여유 확보, 클라이맥스 직전 처짐 제거

## 이 리스트에서 아직 열린 것

- 표지 발표자·일시, 11장 대표 도구 확정(현 claude-mem/playwright/yagni는 예시 placeholder)
- 디자인 세부 조정(개별 장 연출·간격) — 큰 컨셉은 확정(reveal.js + vibe 테마)

## v0.6 변경 이력 (2026-07-07)

- **skill·MCP·plugin 각 1장 할당** (구 7·8 두 장 → 7·8·9 세 장, 총 19장): 장마다 의미(제목)·목적(lead)·구조(파일트리/다이어그램)·동작원리(①②③ 스텝. plugin은 동작 대신 설치·이름표+관계).
- 디자인: reveal.js(vibe 테마) 재구축 반영 — autoshow 스태거, fragment(데모3·정점·클로징), ESC 오버뷰.

## v0.5 변경 이력 (2026-07-07)

> 진단: 두 메시지(A "알고 써보자" / B "방식을 바꿔야")가 섞여 혼란 → **척추=A 확정**, B는 클로징 한 줄로.
> 26-에이전트 감사(장별 3축 비평 + 공식문서 검증 + 구성 진단) 반영.

- **구조**: 데모 1을 정의 앞으로(show→tell) / 구 6+7 병합 / 구 11+12 병합 / 부록 슬라이드 신설 / 섹션 브리지(5→6, 10→11) — 19→18장
- **표기**: MCP 대문자, 정준 순서 skill·MCP·plugin, 데모 1/2/3 라벨, SAME INPUT·auto 등 한글화, '클라이맥스'→'정점'
- **정확성**: Ex2 JSON 스키마 완결, '항상 유효/매번 동일' 과장 제거, harness 공식 용어("agentic harness") 근거 추가, 스키마=assertion 한계 명시, OpenCode=클라이언트로 범주 교정
- **문구**: 캐주얼 표현 일괄 정리(알아서/훑기/결/틀이 있고 없고/딱 하나만 켜서/가르치는 점 등)

## v0.4 콘텐츠 전문화 패스 (2026-07-06)

- 역사 4칸 뭉뚱 → 6개 연도·명칭 마일스톤(검증됨). 레고·부품상자·작업대 비유 폐기 → 정확 정의+동작 2스텝+관계.
- harness를 verification harness(UVM)에 빗대는 재구성 채택. 성능 수치(속도·%)는 계속 미사용.

---

## 부록 A — 데모 2 / 데모 3 공유 parsing 예시 (실제 내용)

> 하나의 "문서 parsing" 작업을 데모 2·데모 3이 공유.
> 아래는 **구성된 예시(illustrative)** — 실제로 참인 메커니즘(스키마 검증 + 재시도가 일관성·품질을 높임)을
> 보여주기 위한 대표 사례. 특정 시스템의 측정값이 아님.
> 원하면 사용자가 자신의 harness 실제 실행 결과로 교체 가능(선택).

### 공유 입력 문서 (회의 메모)

```
[회의 메모]
10/24 주간 동기화. 참석: 지훈, 민아, 세영.
- 로그인 API 응답 지연 이슈, 원인은 토큰 갱신 로직으로 확인. 지훈이 금요일까지 수정.
- 신규 대시보드 디자인 시안 3안 중 2안으로 결정. 민아가 다음 주 월요일까지 반영.
- 다음 회의 10/31 오후 3시.
```

- 중립적·누구나 이해 가능. 설계/검증 특화(UVM·RTL) 아님 → 범용 소개에 적합. (교체 가능)
- 슬라이드 rule에서 도메인 다리 반 줄로 연결("설계·검증에선 스펙 문서·검증 로그가 이 자리에").

### 목표 스키마

```
{ title, date, participants[], decisions[], action_items[{owner, task, due}], next_meeting }
```

### 데모 2 — 기본 동작 (10장)

비정형 문서를 skill이 한 번에 구조화. **잘 되는 케이스만** 보여줌. (v0.5: 스키마 완전 일치 — decisions·task 포함, due는 ISO로 통일. 2024-10-24=목요일 기준 금요일=10-25, 다음 주 월요일=10-28 — 날짜 검증됨)

```json
{
  "title": "주간 동기화",
  "date": "2024-10-24",
  "participants": ["지훈", "민아", "세영"],
  "decisions": ["대시보드 시안 2안으로 결정"],
  "action_items": [
    { "owner": "지훈", "task": "토큰 갱신 수정", "due": "2024-10-25" },
    { "owner": "민아", "task": "대시보드 2안 반영", "due": "2024-10-28" }
  ],
  "next_meeting": "2024-10-31 15:00"
}
```

- 가르치는 점: **비정형 입력 → 구조화 출력**, skill이 하는 일을 눈으로 확인.

### 데모 3 — 같은 작업, harness 유무 (15장)

같은 입력을 **3번씩** 돌린다고 상상. 축은 **품질 + 재현성**.

**harness 없이 (단발 호출, 실행마다 출력이 달라짐)**

- Run 1 — 필드 누락 + 구조 드리프트: `participants`가 배열이 아니라 한 문자열, `action_items`에 `due` 빠짐
  ```json
  { "title": "주간 동기화", "participants": "지훈, 민아, 세영",
    "action_items": ["로그인 API 수정", "대시보드 반영"] }
  ```
- Run 2 — 키 이름이 바뀜(스키마 불일치): 한글 키로 나옴
  ```json
  { "제목": "주간 동기화", "참석자": ["지훈","민아","세영"], "결정": ["2안 채택"] }
  ```
- Run 3 — JSON 깨짐(기계 파싱 불가): 설명 문장이 앞에 붙고 마지막 쉼표 오류
  ```
  다음은 회의 내용을 정리한 결과입니다:
  { "title": "주간 동기화", "date": "2024-10-24", }
  ```

→ **품질**: 누락·형식 오류 발생. **재현성**: 세 번 모두 형태가 다름 → 뒷단에서 받아쓸 수 없음.

**harness로 감싸면 (스키마 검증 + 불합격 시 재생성)**

- Run 1·2·3 — **매번 스키마에 일치**하는 완결 JSON(데모 2와 같은 구조).
  ※ 보장되는 것은 '스키마 일치(구조)'이지 값까지의 결정론이 아님 — 슬라이드도 "같은 구조"로 표기.

→ **품질**: 스키마 게이트를 통과한 것만 나오므로 완결·유효. **재현성**: 세 번 모두 같은 구조.

### 이 대조가 정직한 이유

- LLM 단발 호출의 스키마 미준수(키 누락·드리프트)와 JSON 앞 설명문(preamble)은 **벤더 공식 문서가 인정하는 실제 현상** — 지어낸 결함이 아님.
- harness의 "스키마 정의 → 검증 → 실패 시 재시도"는 출력을 스키마에 수렴시키는 실제 표준 기법.
- 그래서 위 예시는 **참인 현상을 대표 사례로 보여주는 것**. 단, 특정 harness의 측정 수치인 척은 하지 않음.
- 속도/퍼센트 같은 성능 숫자는 쓰지 않음 — 품질·재현성으로만 승부.

---

## 부록 B — 유용한 것들 전체 목록 (슬라이드 19장의 원천)

> 이 목록이 **슬라이드 19장(부록)**으로 실체화됨 — 발표 땐 넘기지 않고, 자료 배포 후 자가학습 진입로.
> 슬라이드 11장은 아래 3칸 중 **각 예 1개씩만** 본문에 노출.

| 카테고리 | 후보 | 본문 예시(11장) |
|----------|------|------------------|
| Context 확장 | ralph loop · claude-mem · superpowers | claude-mem (placeholder) |
| 웹·브라우저 | firecrawl · frontend design · playwright | playwright (placeholder) |
| Token 절약 | caveman · ponytail · yagni | yagni (placeholder) |

- 설치 안내는 슬라이드 11장에서 공통 한 줄("설정에 항목 하나 등록하는 수준").
- 대표(예시) 확정은 실제 사내/사외 환경 검증 후 — 현재는 placeholder.
