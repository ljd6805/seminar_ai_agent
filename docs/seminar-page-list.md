# AI Agent 세미나 — 페이지별 구성 (v0.9)

> 각 페이지가 "무엇을 담는가" 수준의 분해. 시각 표현·디자인은 추후.
> **1시간 30분(약 90분) 기준. 총 42장 = 발표 40장 + 부록 2장(넘기지 않음).**
> **데모는 라이브가 아니라 미리 만든 예시를 자료에 삽입.**
> **데모 2·데모 3은 하나의 "문서 parsing" 작업을 공유** — 데모 2에서 기본 동작, 데모 3에서 같은 작업의 harness 유무 대조.
> 데모 라벨: 청중 노출은 **데모 1/2/3**, 내부 관리번호는 Ex1/Ex2/Ex4 (Ex3=GitOps는 제외된 결번 — 청중에게 노출하지 않음).

---

## 도입 (약 28분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 1 | 표지 | 제목 "AI Agent, 오늘부터 쓸 수 있습니다" · copy "무엇인지 알고 → 왜 좋은지 보고 → 오늘 바로 써보기까지" |
| 2 | 목차 | 5부 여정(도입/본문1/본문2/본문3/마무리) — 데모 1의 성장 얼개와 같은 리스트 컴포넌트. "낯선 단어는 나오는 자리에서 전부 풀어서 + 부록 용어집" 안심 문구 |
| 3 | 역사 Ⅰ — 학습의 시대 | 2012 AlexNet · 2017 Transformer · 2020 GPT-3 · 2022 ChatGPT. 마감: "여기까지는 '말하는 AI' — 아직 행동은 못 함" |
| 4 | 역사 Ⅱ — 행동의 시대 | 2023 function calling · 2024 MCP · 2024–25 agent loop = AI agent. "조합이 갖춰진 게 불과 2~3년" (왜 지금의 복선) |
| 5 | LLM 기초 (3분 기초) | 오늘 쓸 개념 셋 — LLM=다음 말 예측(계산 아닌 예측→틀릴 수 있음) · 컨텍스트=유한한 작업 기억(넣어준 것만 안다) · 토큰=사용량 단위. "확장 요소들이 전부 이 한계를 다루는 장치" 복선 |
| 6 | **데모 1 (Ex1)** ⭐ | (v0.9 소재 교체) **회귀 로그 실패 분류 장면** 애니메이션 (자동재생) — 좌 대화 6버블(수집→유형 제안→대표 케이스→원인 불명은 사람이), 우 "실패 분류 리포트"가 한 줄씩 성장. **정의보다 앞 = show→tell.** 슬라이드에 "구성된 예시 장면" 라벨 명시 |
| 7 | 데모 1 해부 | 방금 화면의 세 장면 → 핵심 3요소 매핑: 표 정리 제안=다단계 · 리포트 성장=도구 사용 · "대표 케이스 붙여줘" 반영=결과 확인. 정의 장의 사전 정지작업 |
| 8 | AI agent란? | "방금 보신 것처럼—" 데모 콜백 정의. 기존 vs agent 2칸 대조 + 요약 띠 "도구 사용 · 다단계 진행 · 결과 확인" |
| 9 | 챗봇 vs agent | 같은 LLM, 차이는 도구+루프 — 챗봇(질문→답변, 말로 끝) vs agent(목표→계획·실행·확인↻→작업물). flow-lane 대조 |
| 10 | 왜 지금 | '지금'의 근거(도구 호출 2023·연결 표준 2024 + 사내 LLM 보유) → 시간 재배분 대조. 브리지: "우리 일과 무슨 상관? 설계·검증의 하루를 봅니다" |
| 11 | 왜 우리 — 설계·검증의 반복 | 도메인 pain 3칸: 스펙·문서(개정판 diff·레지스터 맵 표) / 로그·리포트(원인 줄 찾기·회귀 분류) / 코드·스크립트(테스트벤치 보일러플레이트). "규칙은 분명한데 손이 많이 가는 일 = agent가 가장 잘하는 유형" |
| 12 | 어떻게 — 일의 재배치 | 기존(전 과정 사람) vs 요구되는(정의→agent 실행→확인·판단) flow 대조. 톤=기술(記述), 척추 A 유지. 브리지: "어떤 일부터 맡기면 될까?" |
| 13 | 무엇을 맡기나 | 위임 기준 — 맡기기 좋은 일(확인 쉬운 반복·되돌리기 쉬움) vs 아직 사람(판단 무거움·실수 비쌈·민감 데이터). 기준 한 줄 "결과를 내가 바로 확인할 수 있는가". 민감 데이터 → 본문 1 브리지 |

## 본문 1 — 우리 환경 = 정보 경계 (약 12분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 14 | 간지 | "우리 환경 — 데이터는 어디로 가는가" |
| 15 | 격리 다이어그램 | 사용자 노드 두 갈래 분기 — 위(초록) 사내 AI agent(OpenCode·Roo·DS assistant API), 아래(경고색) 빗금 경계 벽 관통 → 사외 AI agent(Claude, 도입 예정). **보안 원칙 유일한 온전 진술** — 이후 장은 콜백만 |
| 16 | 사내 도구 셋 | OpenCode(터미널 클라이언트) · Roo(VS Code agent) · DS assistant API(직접 호출 API) — 각각 어디서 쓰는 물건인지. 접속 안내 placeholder(`______`) |
| 17 | 보내도 되는 것 · 안 되는 것 | 판단 사례 — 사내 경로: 업무 자료 활용(권한 경계는 유지) / 사외 경로: RTL·스펙·미공개 정보 금지, 공개 자료 수준까지. **"정확한 기준은 팀 보안 규정이 우선" 명시** |

## 본문 2 — 확장 요소: skill · MCP · plugin (약 27분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 18 | 간지 | "확장 요소 — agent에게 능력을 더하는 법" |
| 19 | 맨몸 모델의 한계 | 문제 제기 — 지식(우리 절차 모름) · 연결(사내 시스템에 못 닿음) · 공유(나눌 포장 단위 없음). "절차는 skill, 연결은 MCP, 공유는 plugin" 예고 |
| 20 | skill 개념 | 절차를 파일(SKILL.md)로. 없으면(매번 다시 설명) vs 있으면(한 번 적고 계속) 대조. 비유 "잘 쓰는 사람의 매뉴얼을 팀 서랍에" |
| 21 | skill 구조·동작 | 파일트리(SKILL.md+참조) + progressive disclosure ①이름·설명 상주 ②맞으면 본문 로드 ③참조는 그때만 |
| 22 | MCP 개념 | 개방형 표준, "AI용 USB-C". 없으면(N×N 제각각) vs 있으면(한 번 만들어 어디서나) 대조. OpenAI·Google도 채택 |
| 23 | MCP 구조·동작 | client–server + tools·resources·prompts + ①연결·목록 교환 ②골라 호출 ③결과 수신. 사용 장면 예(이슈 트래커 서버 → "이번 주 내 이슈 정리") |
| 24 | plugin | 의미=skill·MCP·hook을 묶은 배포 단위 · 구조=plugin.json+skills/hooks/+.mcp.json · marketplace 설치·plugin명:이름 꼬리표 |
| 25 | 관계 정리 | 능력(skill)·연결(MCP)·배포(plugin) 3칸 + "plugin은 그릇, 나머지는 내용물(단독 사용 가능)" + 19장 세 구멍과 1:1 대응 콜백 |
| 26 | 데모 2a — SKILL.md 실물 | meeting-notes skill의 실제 내용(frontmatter+절차 5줄) — "코드가 아니라 우리말 절차서". 데모의 중간 고리(입력→**skill**→출력) 완성 |
| 27 | **데모 2b (Ex2) — 실행** | 회의 메모 → 스키마 JSON. JSON은 선언 스키마와 완전 일치(decisions·task 포함, due ISO). rule에 도메인 다리("설계·검증에선 스펙 문서·검증 로그가 이 자리에") |
| 28 | 도구 지도 + 설치·주의 | 3칸 지도(Context 확장/웹·브라우저/Token 절약)×예 1개 + 설치 한 줄(전체 목록은 부록) + 주의 한 줄(외부 연결 → 보안 콜백) |

## 본문 3 — harness (약 18분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 29 | 간지 | "harness — 믿고 쓸 수 있게 만드는 틀" |
| 30 | 실패의 현실 (정직한 전제) | agent도 틀린다 — 실패①그럴듯한 오답(환각) · 실패②들쭉날쭉한 형식(파이프라인이 깨지는 지점). "확인 자체를 틀에 넣을 수 없을까? → harness" 문제 제기 |
| 31 | harness란? | 순서·규칙+검증+재시도의 실행 틀. verification harness와 같은 구조. **공식 근거**: Claude Code 문서의 "agentic harness" |
| 32 | UVM 나란히 | 두 패널 병렬 다이어그램 — sequence→DUT→assertion vs 입력·절차(skill)→모델→스키마 검증. "구조가 같고 대상만 칩에서 모델로" — 도메인 공명의 정점 |
| 33 | 스키마 게이트 | 입력→skill→스키마 검증→통과/실패(한도 내 재시도 ↻). 스키마=assertion처럼 형식 게이트 + 정직한 한계(내용 정합은 별도 — 지금은 사람이) |
| 34 | 장점과 단점 | 품질(검증 통과분만)·재현성(동일 구조) vs 초기 설계·유지보수 비용. 브리지: "언제 치를 가치가 있나?" |
| 35 | 언제 쓰나 | 판단 기준 — 맨 agent(사람이 읽고 끝) vs harness(반복·형식 고정·다음 프로그램의 입력). 기준 한 줄 "출력이 사람에게 가면 맨 agent, 프로그램에게 가면 harness" |
| 36 | **데모 3 (Ex4)** ⭐ | 같은 작업 harness 유무 3회 대조 — 정점. bad run은 실제 깨진 출력 스냅샷(문자열 participants/한글 키/preamble+쉼표 오류). 축=품질+재현성("같은 구조" — 값 결정론 아님) |

## 마무리 (약 12분) + 부록

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 37 | 오늘 지나온 길 (회고) | (v0.9 교체 — 구 "다시 그 장면" 데모 1 콜백 폐기) 3칸 요약: 어디서(데이터는 사내에)·무엇으로(확장 요소 셋)·어떻게(틀로 감싸서, 내용 판단은 사람) + fragment 펀치라인 "어디서 · 무엇으로 · 어떻게. 남은 건 써 보는 것" |
| 38 | FAQ 선제 대응 | Q1 일이 없어지나(반복이 없어짐·판단은 더 사람 몫) · Q2 어디까지 믿나(형식은 틀, 내용은 사람) · Q3 뭘 공부하나(일을 정의하는 연습) |
| 39 | 그래서, 월요일 아침 (CTA) | 딱 하나: 사내 LLM에서 · 스스로 판단 가능한 작은 작업 · 사람이 최종 확인. **첫 프롬프트 실물 터미널**(회의 메모 표 정리 + "불확실은 ?로") 추가 |
| 40 | Q&A 클로징 | A 착지+B 지평선: "원리를 아는 여러분 … // 그 하나가 쌓이면, 일하는 방식은 어느새 바뀌어 있을 겁니다." 지지대 칩(이 자료·부록·동료) |
| 41 | 부록 — 도구 목록 (넘기지 않음) | 확장 요소 후보 전체 목록 — 부록 B의 슬라이드 실체화 |
| 42 | 부록 — 용어집 (넘기지 않음) | 오늘 나온 용어 12개 한 줄 정의(LLM·컨텍스트·토큰·agent·환각·skill / MCP·MCP 서버·plugin·hook·harness·스키마) |

## 페이지 수 요약

- 도입 13 + 본문1 4 + 본문2 11 + 본문3 8 + 마무리 4 + 부록 2 = **42장** (발표 40 + 부록 2)
- 90분 기준 발표 40장 ≈ 장당 2분 여유 — "많이 만들고 줄이기" 전략의 원본. 이후 압축 시 이 문서에서 삭제 이력 관리.

## 이 리스트에서 아직 열린 것

- 표지 발표자·일시, 16장 접속 안내(`______`), 28장 대표 도구 확정(현 claude-mem/playwright/yagni는 예시 placeholder)
- 디자인 세부 조정(개별 장 연출·간격) — 큰 컨셉은 확정(reveal.js + vibe 테마)

## v0.9 변경 이력 (2026-07-07) — "발표자료 제작" 예시 폐기

> 사용자 결정: Ex1의 "이 발표자료도 이렇게 만들었다" 소재가 발표자의 일이지 청중의 일이 아님 — 도입·결말 모두에서 제거.

- **6장 데모 1 소재 교체**: 발표자료 제작 대화 → **회귀 로그 실패 분류 장면**(구성된 예시 라벨 슬라이드에 명시). 채팅 오토플레이·성장 패널 컴포넌트는 재사용(우측 = 실패 분류 리포트). 7장 해부·8장 "방금 보신 것처럼" 콜백 유지.
- **37장 교체**: "다시 그 장면"(데모 1 정지컷 콜백) → **"오늘 지나온 길" 회고 요약**(어디서·무엇으로·어떻게 3칸 + fragment 펀치라인). 40장 클로징 "저 화면의 원리" → "AI agent의 원리".
- 데모 번호 체계 불변(데모 1/2/3, 내부 Ex1/Ex2/Ex4) — Ex1의 소재만 바뀜.

## v0.8 변경 이력 (2026-07-07) — 20장 → 42장 전면 확장

> 발표 시간 60→90분 확정. 비판 분석(서사 구멍 A / 압축 과적 B / 도메인 공명 C) 전면 반영 — "많이 만들고 줄이기" 전략.

- **A 서사 구멍 해소**: 19장 맨몸 한계(문제→해결 구조) · 30장 실패의 현실(신뢰 캘리브레이션을 본문3 초입으로) · 5장 LLM 기초(컨텍스트·토큰 선행 정의) · 26장 SKILL.md 실물(데모의 중간 고리) · 39장 첫 프롬프트 실물
- **B 압축 해체**: 역사 1장→2장(3·4) · skill/MCP 각 1장→개념+구조·동작 2장씩(20~23) · 관계 정리 독립(25) · 12장 4역할 해소(브리지는 간지로)
- **C 도메인 공명**: 11장 설계·검증 pain · 32장 UVM 병렬 다이어그램 · 16~17장 사내 도구 소개+판단 사례
- **구조 장치**: 목차(2) · 간지 3장(14/18/29) · 챗봇 vs agent(9) · 데모 1 해부(7) · 위임 기준(13) · 언제 쓰나(35) · FAQ(38) · 용어집(42)
- 데모 3 bad run을 텍스트 설명 → 실제 깨진 출력 스냅샷으로 교체. fragment 위치는 36/37/40장으로 이동(내용 불변).

## v0.7 변경 이력 (2026-07-07)

- **도입에 6장 신설 "어떻게 해야 하나 — 일의 재배치"** (총 20장): 기존 vs 요구되는 작업 방식 flow 대조 + "왜 agent인가"(반복→판단 재배치).
- 우리 환경을 격리 다이어그램으로 재구성 — 사용자 노드 분기(사내 AI agent: OpenCode·Roo·DS assistant API / 사외 AI agent: Claude), 빗금 경계 벽 관통 화살표.

## v0.6 변경 이력 (2026-07-07)

- **skill·MCP·plugin 각 1장 할당** (총 19장): 장마다 의미·목적·구조·동작원리.
- 디자인: reveal.js(vibe 테마) 재구축 반영 — autoshow 스태거, fragment, ESC 오버뷰.

## v0.5 변경 이력 (2026-07-07)

> 진단: 두 메시지(A "알고 써보자" / B "방식을 바꿔야")가 섞여 혼란 → **척추=A 확정**, B는 클로징 한 줄로.
> 26-에이전트 감사(장별 3축 비평 + 공식문서 검증 + 구성 진단) 반영.

- **구조**: 데모 1을 정의 앞으로(show→tell) / 구 6+7 병합 / 구 11+12 병합 / 부록 슬라이드 신설 — 19→18장
- **표기**: MCP 대문자, 정준 순서 skill·MCP·plugin, 데모 1/2/3 라벨, 영어 라벨 한글화, '클라이맥스'→'정점'
- **정확성**: Ex2 JSON 스키마 완결, '항상 유효/매번 동일' 과장 제거, harness 공식 용어("agentic harness") 근거 추가, 스키마=assertion 한계 명시, OpenCode=클라이언트로 범주 교정

## v0.4 콘텐츠 전문화 패스 (2026-07-06)

- 역사 4칸 뭉뚱 → 6개 연도·명칭 마일스톤(검증됨). 레고·부품상자·작업대 비유 폐기 → 정확 정의+동작 2스텝+관계.
- harness를 verification harness(UVM)에 빗대는 재구성 채택. 성능 수치(속도·%)는 계속 미사용.

---

## 부록 A — 데모 2 / 데모 3 공유 parsing 예시 (실제 내용)

> 하나의 "문서 parsing" 작업을 데모 2·데모 3이 공유.
> 아래는 **구성된 예시(illustrative)** — 실제로 참인 메커니즘(스키마 검증 + 재시도가 일관성·품질을 높임)을
> 보여주기 위한 대표 사례. 특정 시스템의 측정값이 아님.
> 원하면 사용자가 자신의 harness 실제 실행 결과로 교체 가능(선택).

### 공유 입력 문서 (회의 메모)

```
[회의 메모]
10/24 주간 동기화. 참석: 지훈, 민아, 세영.
- 로그인 API 응답 지연 이슈, 원인은 토큰 갱신 로직으로 확인. 지훈이 금요일까지 수정.
- 신규 대시보드 디자인 시안 3안 중 2안으로 결정. 민아가 다음 주 월요일까지 반영.
- 다음 회의 10/31 오후 3시.
```

- 중립적·누구나 이해 가능. 설계/검증 특화(UVM·RTL) 아님 → 범용 소개에 적합. (교체 가능)
- 슬라이드 rule에서 도메인 다리 반 줄로 연결("설계·검증에선 스펙 문서·검증 로그가 이 자리에").

### 사용 skill (26장에서 실물 공개)

```
meeting-notes/SKILL.md — name·description frontmatter + 절차 5줄
(제목·날짜·참석자 추출 / decisions / action_items{owner,task,due} / 날짜 YYYY-MM-DD / next_meeting)
```

### 목표 스키마

```
{ title, date, participants[], decisions[], action_items[{owner, task, due}], next_meeting }
```

### 데모 2 — 기본 동작 (27장)

비정형 문서를 skill이 한 번에 구조화. **잘 되는 케이스만** 보여줌. (v0.5: 스키마 완전 일치 — decisions·task 포함, due는 ISO로 통일. 2024-10-24=목요일 기준 금요일=10-25, 다음 주 월요일=10-28 — 날짜 검증됨)

```json
{
  "title": "주간 동기화",
  "date": "2024-10-24",
  "participants": ["지훈", "민아", "세영"],
  "decisions": ["대시보드 시안 2안으로 결정"],
  "action_items": [
    { "owner": "지훈", "task": "토큰 갱신 수정", "due": "2024-10-25" },
    { "owner": "민아", "task": "대시보드 2안 반영", "due": "2024-10-28" }
  ],
  "next_meeting": "2024-10-31 15:00"
}
```

- 가르치는 점: **비정형 입력 → 구조화 출력**, skill이 하는 일을 눈으로 확인.

### 데모 3 — 같은 작업, harness 유무 (36장)

같은 입력을 **3번씩** 돌린다고 상상. 축은 **품질 + 재현성**. (v0.8: 슬라이드에도 아래 스냅샷을 실제 텍스트로 노출)

**harness 없이 (단발 호출, 실행마다 출력이 달라짐)**

- Run 1 — 필드 누락 + 구조 드리프트: `participants`가 배열이 아니라 한 문자열, `action_items`에 `due` 빠짐
  ```json
  { "title": "주간 동기화", "participants": "지훈, 민아, 세영",
    "action_items": ["로그인 API 수정", "대시보드 반영"] }
  ```
- Run 2 — 키 이름이 바뀜(스키마 불일치): 한글 키로 나옴
  ```json
  { "제목": "주간 동기화", "참석자": ["지훈","민아","세영"], "결정": ["2안 채택"] }
  ```
- Run 3 — JSON 깨짐(기계 파싱 불가): 설명 문장이 앞에 붙고 마지막 쉼표 오류
  ```
  다음은 회의 내용을 정리한 결과입니다:
  { "title": "주간 동기화", "date": "2024-10-24", }
  ```

→ **품질**: 누락·형식 오류 발생. **재현성**: 세 번 모두 형태가 다름 → 뒷단에서 받아쓸 수 없음.

**harness로 감싸면 (스키마 검증 + 불합격 시 재생성)**

- Run 1·2·3 — **매번 스키마에 일치**하는 완결 JSON(데모 2와 같은 구조).
  ※ 보장되는 것은 '스키마 일치(구조)'이지 값까지의 결정론이 아님 — 슬라이드도 "같은 구조"로 표기.

→ **품질**: 스키마 게이트를 통과한 것만 나오므로 완결·유효. **재현성**: 세 번 모두 같은 구조.

### 이 대조가 정직한 이유

- LLM 단발 호출의 스키마 미준수(키 누락·드리프트)와 JSON 앞 설명문(preamble)은 **벤더 공식 문서가 인정하는 실제 현상** — 지어낸 결함이 아님.
- harness의 "스키마 정의 → 검증 → 실패 시 재시도"는 출력을 스키마에 수렴시키는 실제 표준 기법.
- 그래서 위 예시는 **참인 현상을 대표 사례로 보여주는 것**. 단, 특정 harness의 측정 수치인 척은 하지 않음.
- 속도/퍼센트 같은 성능 숫자는 쓰지 않음 — 품질·재현성으로만 승부.

---

## 부록 B — 유용한 것들 전체 목록 (슬라이드 41장의 원천)

> 이 목록이 **슬라이드 41장(부록)**으로 실체화됨 — 발표 땐 넘기지 않고, 자료 배포 후 자가학습 진입로.
> 슬라이드 28장은 아래 3칸 중 **각 예 1개씩만** 본문에 노출.

| 카테고리 | 후보 | 본문 예시(28장) |
|----------|------|------------------|
| Context 확장 | ralph loop · claude-mem · superpowers | claude-mem (placeholder) |
| 웹·브라우저 | firecrawl · frontend design · playwright | playwright (placeholder) |
| Token 절약 | caveman · ponytail · yagni | yagni (placeholder) |

- 설치 안내는 슬라이드 28장에서 공통 한 줄("설정에 항목 하나 등록하는 수준").
- 대표(예시) 확정은 실제 사내/사외 환경 검증 후 — 현재는 placeholder.
