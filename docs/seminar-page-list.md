# AI Agent 세미나 — 페이지별 구성 (v0.15)

> 각 페이지가 "무엇을 담는가" 수준의 분해. 시각 표현·디자인은 추후.
> **1시간 30분(약 90분) 기준. 총 53장 = 발표 51장 + 부록 2장(넘기지 않음).**
> **데모는 라이브가 아니라 미리 만든 예시를 자료에 삽입.**
> 데모 4개: 데모 1(도입 훅) · 데모 2(skill) · 데모 3(MCP) · 데모 4(harness 유무). **데모 2·데모 4는 하나의 "문서 parsing" 작업을 공유.**
> 데모 라벨: 청중 노출은 **데모 1~4**, 내부 관리번호는 Ex1(데모 1)·Ex2(데모 2)·Ex5(데모 3, v0.11 신규)·Ex4(데모 4). Ex3=GitOps는 제외된 결번 — 청중에게 노출하지 않음.
> **발표 대본은 [speaker-notes.md](speaker-notes.md)** — 장별 핵심 문장·오해 바로잡기 6·90분 배분.

---

## 도입 (약 30분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 1 | 표지 | 제목 "AI Agent, 오늘부터 쓸 수 있습니다" · copy "무엇인지 알고 → 왜 좋은지 보고 → 오늘 바로 써보기까지" |
| 2 | 목차 | 5부 여정(도입/본문1/본문2/본문3/마무리) — "낯선 단어는 나오는 자리에서 전부 풀어서 + 부록 용어집" 안심 문구. 본문 3 라벨 = "형식과 실행을 통제하는 틀" |
| 3 | **오늘의 결론** (v0.15 신설) | 결론 선행 — 인용 박스 "AI agent는 사람의 판단을 대체하는 도구가 아니라, 판단 전에 필요한 반복 작업을 줄이는 도구입니다" + 쉬운 업무 예(regression 로그 분류·회의록 action item·spec 변경점 요약) + **관통 4문장 목록**(① agent=LLM+도구+루프 실행자 ② skill=일하는 방법 ③ MCP=시스템 연결 ④ harness=깨지지 않게 감싸기). note = 목표는 완벽한 이해가 아니라 "이번 주 검증 가능한 작은 업무 하나 적용" |
| 4 | AI의 발전 (역사) | 7노드 타임라인. **표현 보수화(v0.15)**: AlexNet="GPU 딥러닝이 대규모 이미지 인식에서 실용성을 증명한 상징적 사건" · function calling="대표 API 패턴" · MCP="공용 규격으로 확산 중" · agent loop="정해진 한도 안에서 반복하는 루프". lead 두 시대 구분, note "불과 2~3년" + 기초 4장 브리지 |
| 5 | 기초 ① LLM | 다음 말 예측 애니메이션(1회 재생+↻). 캡션에 **"엄밀히는 다음 토큰 예측 — 오늘은 이해를 돕기 위해 다음 말로"** 각주(v0.15). note = 예측이라 유창하지만 답이 항상 사실은 아님 — 근거 확인·검증 필요 |
| 6 | 기초 ② 컨텍스트 | 차오르는 창 애니메이션(1회 재생+↻). 캡션(v0.15) = **"한도를 넘으면 일부가 잘리거나 요약되고, 길수록 관련 정보를 집어내기 어려움"**(사람 기억 비유 완화). note = 필요한 절차·자료만 골라 넣기 — skill이 그 정리법(재사용 가능한 작업 지침) 복선 |
| 7 | 기초 ③ 토큰 | 간결 유지 — kWh 비유 + 조각내기 애니메이션(1회 재생+↻). note(v0.15) = **실무 요령**: "수십 MB regression log 전체보다 fail 주변+summary만 — 많이 넣기보다 잘 고르기" |
| 8 | 기초 ④ 환각 | 대화 3박자 애니메이션(1회 재생+↻, 구성된 예시 라벨). lead(v0.15) = "모른다고 멈추기보다 그럴듯한 답으로 기울 수 있다 — 예측 방식에서 오는 한계". note = **"유창함은 신뢰성과 다릅니다"** + 근거·도구·검증 절차로 감싸기(harness 복선) + "알고 쓰자" |
| 9 | **데모 1 (Ex1)** ⭐ | regression 로그 실패 분류 장면(1회 시퀀스 재생+↻, 구성된 예시 라벨) — 좌 대화 6버블 → 우 리포트 5행. show→tell |
| 10 | 데모 1 해부 | 세 장면 → 핵심 3요소 매핑(다단계·도구 사용·결과 확인) |
| 11 | AI agent란? | 정의(v0.15) = **"정해진 권한과 한도 안에서, 이해→계획→도구 사용→확인을 반복"** + 등식·순환 레인 애니메이션(1회 재생+↻). 루프 문구 "확인하고 다시 시도(한도 내)", note = "마법이 아니라 권한과 한도 안의 자동화 실행 흐름" |
| 12 | 챗봇 vs agent | 같은 LLM, 차이는 도구+루프. **공정 대조(v0.15)**: "묻고 답하기엔 챗봇으로 충분" / note 기준 한 줄 = "질문에 답하면 챗봇, 목표를 받아 도구를 쓰며 진행하면 agent" |
| 13 | 왜 지금 | 도구 호출 2023 · 연결 규격 확산 2024 + 사내 LLM 보유 → 시간 재배분 대조 |
| 14 | 왜 우리 — 설계·검증의 반복 | 도메인 pain 3칸: 스펙·문서 / 로그·리포트 / 코드·스크립트 |
| 15 | 어떻게 — 일의 재배치 | 기존(전 과정 사람) vs 요구되는(정의→agent 실행→확인·판단) flow 대조 |
| 16 | 무엇을 맡기나 | 제목(v0.15) "처음에는 — 내가 바로 검증할 수 있는 일부터". 좋은 일 예 = regression 실패 분류·spec 변경점 요약·review comment 정리·register table 변환·회의록 action item / 조심할 일 예 = sign-off·architecture 결정·RTL 의미 변경·root cause 확정·보안/고객 커뮤니케이션. note = **"AI가 잘하는 건 초안·분류·변환·요약 — 최종 판단은 사람"** + 본문 1 브리지 |

## 본문 1 — 우리 환경 = 정보 경계 (약 12분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 17 | 간지 | "우리 환경 — 데이터는 어디로 가는가" |
| 18 | 격리 다이어그램 | 사용자 노드 두 갈래 분기(사내 초록/사외 경고, 경계 벽). 보안 원칙 온전 진술 — quote에 **"사내라도 내 접근 권한 안에서만"** 추가(v0.15) |
| 19 | 사내 도구 셋 | OpenCode · Roo · DS assistant API. 접속 안내 placeholder(`______`) |
| 20 | 보내도 되는 것 · 안 되는 것 | 사내 카드(v0.15) = "권한 안의 업무 자료만 — 프로젝트 경계·need-to-know 유지" / 사외 카드 = 금지 목록 구체화(RTL·미공개 spec·고객 정보·일정·내부 이슈/로그 원문·내부 경로·서버명·계정). note = **실무 체크 "외부 메일로 보내도 되는가? 아니오면 사외 AI에도 금지"** + "정확한 기준은 팀 보안 규정이 우선" |
| 21 | **agent 사용 전 3문장 점검** (v0.15 신설) | 3카드 체크리스트 — ① 내가 접근 권한을 가진 자료인가(사내라도 경계·need-to-know 유지) ② 사외로 나가도 되는가(외부엔 RTL·spec·고객·이슈 금지) ③ 틀렸을 때 바로 알아챌 수 있는가(검증 가능한 작은 작업부터). note = 셋 다 "예"면 도구를 켤 차례 + 팀 규정 우선 |
| 22 | OpenCode 설치 | 리눅스 설치 터미널 애니메이션(1회 재생+↻, PC 셸 룩) — 주소 placeholder. note = 사내 안내 페이지 placeholder + 본문 2 브리지 |

## 본문 2 — 확장 요소: skill · MCP (약 28분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 23 | 간지 | "확장 요소 — agent에게 능력을 더하는 법" |
| 24 | 기본 모델의 한계 | 두 공백(절차·연결) + skill/MCP 대칭 배너 → "절차는 skill, 연결은 MCP" |
| 25 | 데모 2a — ① 업무 정의 | 손으로 하던 일 3단계 + 정의 박스. "정의할 수 있으면 맡길 수 있다" |
| 26 | **데모 2b (Ex2) — ② skill 저장 → 실행·활용** | 좌 저장(SKILL.md 실물 — description "정해진 JSON 형식으로"로 표현 완화 v0.15) / 우 실행 opencode 세션(1회 재생+↻) / 하단 주간 리포트 활용 |
| 27 | skill 개념 | 없으면/있으면 대조. note(v0.15) = **"매번 프롬프트를 길게 쓰지 않기 위한 팀 작업 매뉴얼"** + 좋은 skill이 정하는 것 6(언제·입력·순서·출력·금지·검증) |
| 28 | skill 구조·동작 | 파일트리(OpenCode 규격) + 만드는 법 3걸음 + SKILL.md 해부 + progressive disclosure. note 브리지 = "이 규격, 우리 일로 옮겨 봅니다"(v0.15 변경) |
| 29 | **skill 예시 — regression-failure-triage** (v0.15 신설) | 설계/검증 실전 skill 실물 — SKILL.md(name·description + 절차 5: test/seed/timestamp 추출 → timeout·assertion·build·env·unknown 분류 → signature 그룹핑 → 대표 로그 ≤3 인용 → **불확실하면 unknown, 추정 금지** + 출력 Markdown table 5열). 우측 = "달라진 건 소재뿐(규격은 데모 2와 동일)" 카드 + 인용 "추정하지 말고 로그에 근거가 있는 내용만 — 환각을 절차로 누르는 한 줄" |
| 30 | **좋은 skill / 나쁜 skill** (v0.15 신설) | 2카드 5항목 대조 — 좋은: 언제(description 명확)·입출력 형식·순서·금지("추정 금지")·검증 기준 / 나쁜: 모호("알아서 잘")·무범위·자유형·무제약·무검증. note = **"AI에게 맡기는 작업일수록 사람보다 더 명확한 작업 정의가 필요"** + 공백 ② 브리지 |
| 31 | 데모 3a (Ex5) — MCP 없을 때 | 데이터 여정 애니메이션(1회 재생+↻) — 연결=사람, 수동 복붙 반복 |
| 32 | 데모 3b (Ex5) — MCP 있을 때 | 같은 프롬프트, 연결=MCP 서버(jira_search). note(v0.15) = **"자동화된 건 조회까지 — 우선순위 판단·의사결정은 사람이 확인"** |
| 33 | MCP 개념 | 표현 완화(v0.15): "AI application과 외부 도구·시스템을 잇는 **공용 규격**" · "지원하는 클라이언트에서는 재사용이 쉬워짐" · "OpenAI·Google도 채택해 확산 중". note = **"MCP는 손을 달아주는 것이지 판단력을 보장하는 것이 아님"** |
| 34 | MCP 구조·동작 | client–server 3정거장 + opencode.json 실물 + 동작 flow. 캡션(v0.15) = "단, 인증·권한·네트워크·보안 정책은 별도 설계". note = 두 공백 모두 채워짐 |
| 35 | **MCP 통제** (v0.15 신설) | 위험·설명·대응 6행 표 — 너무 많은 tool/권한 과다/prompt injection/데이터 유출/side effect 실행/호출 로그 부재 → 필요한 것만·least privilege·tool 결과 불신·승인된 사내 서버·human approval gate·호출 로그. note = **"agent는 권한을 가진 자동화 실행기 — 권한 설계가 곧 안전 설계"** |
| 36 | 도구 지도 + 설치·주의 | 3칸 지도 × 예 1개(placeholder) + 설치 한 줄 + 외부 연결 주의 |

## 본문 3 — harness (약 20분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 37 | 간지 | "harness — **형식과 실행을 통제하는 틀**"(v0.15, '믿고 쓰는 틀' 폐기) |
| 38 | 실패의 현실 | 환각 콜백 · 들쭉날쭉한 형식 → "확인 자체를 틀에" |
| 39 | harness란? | 순서·규칙+검증+재시도 → "형식이 검증된 실행". 공식 근거 "agentic harness" |
| 40 | UVM 나란히 | 병렬 다이어그램 + 캡션(v0.15): 좌 "최종 spec 해석은 엔지니어가" / 우 "최종 의미·업무 판단은 사람이" |
| 41 | **harness 구성 요소** (v0.15 신설) | 3카드 9항목 — 입력(전처리·skill 선택·tool allowlist) / 출력(schema·validator·retry limit) / 운영(human approval gate·실행 로그 tracing·실패 리포트), 각 한 줄 설명. note = **"모델을 믿기 위한 장치가 아니라, 모델 사용을 통제하기 위한 장치"** |
| 42 | 스키마 게이트 | 게이트 flow + 통과/실패 카드(통과 = 누락 필드 검출·파싱 실패 감소). note(v0.15) = **"스키마는 모양을 검증 — JSON이 맞는지 보는 것이지 내용이 참인지 보는 것이 아님"** + 근거 없는 내용도 올바른 JSON일 수 있음 + assertion↔spec 리뷰 비유 |
| 43 | 장점과 단점 | 재현성 정의(v0.15) = **"매번 같은 답이 아니라 매번 같은 검증 절차·같은 출력 구조를 통과한다는 뜻"**. note = LLM 출력은 확률적 — 구조·검증·로그·승인 절차가 중요 |
| 44 | 언제 쓰나 | "사람에게 가면 agent 그대로, 프로그램에게 가면 harness" + 레벨 브리지 |
| 45 | **AI 자동화 레벨 L0~L5** (v0.15 신설) | 사다리 6행 — L0 질문답변 / L1 파일 요약(데모 1) / L2 형식 산출물(데모 2) / L3 도구 호출(MCP, 데모 3) / L4 harness 자동화(데모 4) / L5 승인 포함 실행(human approval). note = **"처음부터 L5로 가면 사고도 자동화 — 시작은 L1~L2"** + 데모 4 브리지 |
| 46 | **데모 4 (Ex4)** ⭐ | harness 유무 3회 대조 — 축=품질+재현성("같은 구조"까지만) |

## 마무리 (약 10분) + 부록

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 47 | 오늘 지나온 길 (회고) | 어디서·무엇으로(두 공백 콜백)·어떻게 + 펀치라인 |
| 48 | FAQ 선제 대응 | Q1 일이 없어지나 · Q2 어디까지 믿나 · Q3 뭘 공부하나. note 브리지 = "부탁하는 요령 하나 + 할 일"(v0.15) |
| 49 | **좋은 요청 / 나쁜 요청** (v0.15 신설) | 3쌍 대조(runs bad/good) — 로그("분석해줘" vs error signature 그룹+근거 2줄+unknown) / 스펙("정리해줘" vs register field·reset value·access type 표+없는 값 ?) / 코드("고쳐줘" vs validation만·interface 유지·테스트 먼저). note = **"작게 · 구체적으로 · 검증 가능하게"** |
| 50 | 그래서, 어떻게 시작하나 (CTA) | lead(v0.15) = "이번 주, 내가 바로 검증할 수 있는 작은 작업 딱 하나부터". 실습 후보 = 회의록 action item·regression 실패 분류·spec 변경점 요약. **첫 프롬프트 2종**(회의 메모 + regression log/error signature/unknown). note = **"첫 목표는 자동화가 아니라 검증 가능한 반복 작업 하나 줄이기"** + 보안 먼저 |
| 51 | Q&A 클로징 | A 착지+B 지평선. 지지대 칩 |
| 52 | 부록 — 도구 목록 (넘기지 않음) | 확장 요소 후보 전체 — 부록 B의 실체화 |
| 53 | 부록 — 용어집 (넘기지 않음) | 용어 10개 — MCP "공용 규격"·harness "통제하는 실행 틀, 진실성 보장 아님"으로 표현 정합(v0.15) |

## 페이지 수 요약

- 도입 16 + 본문1 6 + 본문2 14 + 본문3 10 + 마무리 5 + 부록 2 = **53장** (발표 51 + 부록 2)
- fragment 없음(전부 autoshow) — 장 넘김 1회 = 1장

## 이 리스트에서 아직 열린 것

- 표지 발표자·일시, 19·22장 접속·설치 안내(`______`), 36장 대표 도구 확정(현 claude-mem/playwright/yagni는 예시 placeholder)
- 리허설 후 압축(줄이기) 대상 선정 — 53장은 90분에 빡빡, 후보: 역사(4)·왜 지금(13)·도구 지도(36) 축약
- 디자인 세부 조정 — 큰 컨셉은 확정(reveal.js + vibe 테마)

## v0.15 변경 이력 (2026-07-09) — 심층 리뷰 반영 (45→53장)

> 사용자 제공 심층 리뷰 28개 항목 반영. 핵심 메시지 고정: "AI agent는 판단을 대체하는 도구가 아니라, 판단 전에 필요한 반복 작업을 줄이는 도구" + 관통 4문장(agent 실행자·skill 방법·MCP 연결·harness 감싸기).

- **신규 8장**: 3 오늘의 결론 · 21 사용 전 3문장 점검 · 29 regression-failure-triage skill · 30 좋은/나쁜 skill · 35 MCP 통제 · 41 harness 구성 요소 · 45 자동화 레벨 L0~L5 · 49 좋은/나쁜 요청
- **표현 보수화(톤 조정)**: 역사(AlexNet 상징적 사건·function calling 대표 패턴·MCP 확산 중), "될 때까지/알아서/어디서나/믿고 쓰는/표준 방법" 계열 제거 → "정해진 권한과 한도 안에서·재사용 가능성·지원하는 환경에서는·통제하는 틀"
- **정확성 보강**: LLM 다음 토큰 각주 · 컨텍스트 잘림/요약·집어내기 어려움 · 재현성="같은 검증 절차·같은 구조 통과" · 스키마="모양 검증, 의미는 별도" · MCP="연결 규격이지 보안 정책 아님(인증·권한 별도)"
- **보안 강화**: "사내라도 접근 권한 안에서만"(18·20) · 사외 금지 목록 구체화 · 외부 메일 체크 문구 · need-to-know
- **발표자 노트 신설**: docs/speaker-notes.md (장별 4구조 + 오해 6 + 90분 배분)

## v0.14 변경 이력 (2026-07-09) — OpenCode 설치 장 신설

> 사용자 결정: 판단 사례 뒤에 리눅스 기준 OpenCode 설치 시나리오를 터미널 애니메이션으로 추가.

- 설치 장 신설: `curl … | bash` 타이핑 애니메이션 → 설치 완료 → `cd` → `opencode` 실행 → 대기 커서. 이후 장 번호 +1, 총 44→**45장**.

## v0.13 변경 이력 (2026-07-09) — 데모 3 2장 분할 (없을 때/있을 때)

> 사용자 결정: MCP 유무 대조 1장을 2장으로. 앞서 skill·MCP 트랙 show→tell 재배열, OpenCode 규격 반영도 이번 주기.

- 데모 3a(없을 때)/3b(있을 때) 분할, 이후 장 번호 +1, 총 43→**44장**.

## v0.12 변경 이력 (2026-07-08) — 도입 기초 1장 → 4장 분리

- LLM·컨텍스트·토큰·환각 각 1장 + 전 장 1회 재생 애니메이션(+↻). agent 차이는 정의 장의 등식·루프로 결합. 총 40→43장.

## v0.11 변경 이력 (2026-07-07) — plugin 제외 + 요소별 데모 배치

- plugin 장 삭제, 확장 요소 = skill·MCP 둘. 데모 3(Ex5) 신설, harness 데모는 데모 4로 재라벨. 총 41→40장.

## v0.10 이전 이력 (요약)

- v0.10: 역사 병합(42→41), 데모 1 정적화 / v0.9: "발표자료 제작" 예시 폐기(데모 1 = regression 로그 분류), "월요일" 제거 / v0.8: 20→42장 대확장(90분 확정) / v0.5~0.7: 척추 확정·show→tell·표기 규칙·격리 다이어그램

---

## 부록 A — 데모 2 / 데모 4 공유 parsing 예시 (실제 내용)

> 하나의 "문서 parsing" 작업을 데모 2(skill)·데모 4(harness)가 공유.
> 아래는 **구성된 예시(illustrative)** — 실제로 참인 메커니즘(스키마 검증 + 재시도가 일관성·품질을 높임)을
> 보여주기 위한 대표 사례. 특정 시스템의 측정값이 아님.
> 원하면 사용자가 자신의 harness 실제 실행 결과로 교체 가능(선택).

### 공유 입력 문서 (회의 메모)

```
[회의 메모]
10/24 주간 동기화. 참석: 지훈, 민아, 세영.
- 로그인 API 응답 지연 이슈, 원인은 토큰 갱신 로직으로 확인. 지훈이 금요일까지 수정.
- 신규 대시보드 디자인 시안 3안 중 2안으로 결정. 민아가 다음 주 월요일까지 반영.
- 다음 회의 10/31 오후 3시.
```

- 중립적·누구나 이해 가능. 설계/검증 특화(UVM·RTL) 아님 → 범용 소개에 적합. (교체 가능)
- 도메인 특화 skill 예시는 **29장 regression-failure-triage**가 담당(v0.15).

### 사용 skill (26장 저장 · 28장 해부에서 실물 공개)

```
meeting-notes/SKILL.md — name·description frontmatter + 절차 5줄
(제목·날짜·참석자 추출 / decisions / action_items{owner,task,due} / 날짜 YYYY-MM-DD / next_meeting)
```

### 목표 스키마

```
{ title, date, participants[], decisions[], action_items[{owner, task, due}], next_meeting }
```

### 데모 2 — 기본 동작 (26장)

비정형 문서를 skill이 한 번에 구조화. **잘 되는 케이스만** 보여줌. (스키마 완전 일치 — decisions·task 포함, due는 ISO로 통일. 2024-10-24=목요일 기준 금요일=10-25, 다음 주 월요일=10-28 — 날짜 검증됨)

```json
{
  "title": "주간 동기화",
  "date": "2024-10-24",
  "participants": ["지훈", "민아", "세영"],
  "decisions": ["대시보드 시안 2안으로 결정"],
  "action_items": [
    { "owner": "지훈", "task": "토큰 갱신 수정", "due": "2024-10-25" },
    { "owner": "민아", "task": "대시보드 2안 반영", "due": "2024-10-28" }
  ],
  "next_meeting": "2024-10-31 15:00"
}
```

### 데모 3 — MCP 연결 (31–32장, Ex5, 예시 시스템 = Jira)

Jira MCP 서버를 연결해 둔 상태에서 "이번 주 내가 맡은 이슈, 상태별로 정리해줘" → agent가 jira_search tool 호출 → 6건 수신 → 상태별 표(진행 중 2 · 리뷰 대기 3 · 막힘 1). **구성된 예시** — 이슈 ID(UI-142 등)는 가상. 가르치는 점: 연결(조회·로그인·복사)이 사람 손에서 사라짐 — 단, 판단·의사결정은 사람 확인.

### 데모 4 — 같은 작업, harness 유무 (46장, Ex4)

같은 입력을 **3번씩** 돌린다고 상상. 축은 **품질 + 재현성**. (슬라이드에도 아래 스냅샷을 실제 텍스트로 노출)

**harness 없이 (단발 호출, 실행마다 출력이 달라짐)**

- Run 1 — 필드 누락 + 구조 드리프트: `participants`가 배열이 아니라 한 문자열, `action_items`에 `due` 빠짐
  ```json
  { "title": "주간 동기화", "participants": "지훈, 민아, 세영",
    "action_items": ["로그인 API 수정", "대시보드 반영"] }
  ```
- Run 2 — 키 이름이 바뀜(스키마 불일치): 한글 키로 나옴
  ```json
  { "제목": "주간 동기화", "참석자": ["지훈","민아","세영"], "결정": ["2안 채택"] }
  ```
- Run 3 — JSON 깨짐(기계 파싱 불가): 설명 문장이 앞에 붙고 마지막 쉼표 오류
  ```
  다음은 회의 내용을 정리한 결과입니다:
  { "title": "주간 동기화", "date": "2024-10-24", }
  ```

→ **품질**: 누락·형식 오류 발생. **재현성**: 세 번 모두 형태가 다름 → 뒷단에서 받아쓸 수 없음.

**harness로 감싸면 (스키마 검증 + 불합격 시 재생성)**

- Run 1·2·3 — **매번 스키마에 일치**하는 완결 JSON(데모 2와 같은 구조).
  ※ 보장되는 것은 '스키마 일치(구조)'이지 값까지의 결정론이 아님 — 슬라이드도 "같은 구조"로 표기.

### 이 대조가 정직한 이유

- LLM 단발 호출의 스키마 미준수(키 누락·드리프트)와 JSON 앞 설명문(preamble)은 **벤더 공식 문서가 인정하는 실제 현상** — 지어낸 결함이 아님.
- harness의 "스키마 정의 → 검증 → 실패 시 재시도"는 출력을 스키마에 수렴시키는 실제 표준 기법.
- 그래서 위 예시는 **참인 현상을 대표 사례로 보여주는 것**. 단, 특정 harness의 측정 수치인 척은 하지 않음.
- 속도/퍼센트 같은 성능 숫자는 쓰지 않음 — 품질·재현성으로만 승부.

---

## 부록 B — 유용한 것들 전체 목록 (슬라이드 52장의 원천)

> 이 목록이 **슬라이드 52장(부록)**으로 실체화됨 — 발표 땐 넘기지 않고, 자료 배포 후 자가학습 진입로.
> 슬라이드 36장은 아래 3칸 중 **각 예 1개씩만** 본문에 노출.

| 카테고리 | 후보 | 본문 예시(36장) |
|----------|------|------------------|
| Context 확장 | ralph loop · claude-mem · superpowers | claude-mem (placeholder) |
| 웹·브라우저 | firecrawl · frontend design · playwright | playwright (placeholder) |
| Token 절약 | caveman · ponytail · yagni | yagni (placeholder) |

- 설치 안내는 슬라이드 36장에서 공통 한 줄("설정에 항목 하나 등록하는 수준").
- 대표(예시) 확정은 실제 사내/사외 환경 검증 후 — 현재는 placeholder.
