# AI Agent 세미나 — 페이지별 구성 (v0.4)

> 각 페이지가 "무엇을 담는가" 수준의 분해. 시각 표현·디자인은 추후.
> 1시간(약 60분) 기준.
> **데모는 라이브가 아니라 미리 만든 예시를 자료에 삽입.**
> **Ex2·Ex4는 하나의 "문서 parsing" 작업을 공유** — Ex2에서 기본 동작, Ex4에서 같은 작업의 harness 유무 대조.

---

## 도입 (약 10분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 1 | 표지 | 제목 · 발표자 · 한 줄 카피 |
| 2 | AI의 역사 | agent의 뿌리, **6개 구체 마일스톤** 타임라인 — 2012 AlexNet · 2017 Transformer · 2020 GPT-3(few-shot) · 2022 ChatGPT(instruction tuning·RLHF) · 2023 function calling · 2024–25 MCP·agent. 각 칸이 "왜 agent로 이어지나"를 한 줄로 (연도·명칭은 공식 출처 검증) |
| 3 | AI agent란? | 정의 — 기존 방식과 뭐가 다른가 |
| 4 | **Ex1 데모** ⭐ | *이 발표자료를 만든 대화 과정* 애니메이션 (자동재생) = 패러다임 전환의 증명 |
| 5 | 왜 지금 우리가 | 바뀔 때 vs 안 바뀔 때, 필요성 |

## 본문 1 — 사내/사외 정보 흐름 (약 7분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 6 | 우리 환경 두 갈래 | 사내 LLM(API·OpenCode) vs 사외 Claude Code |
| 7 | 정보는 어디로 가나 | 사내에 머무는가 / 사외로 나가는가 — 데이터 경계 시각화 |

## 본문 2 — plugin / skill / mcp (약 20분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 8 | 개념: 셋이 뭔가 | **전문 정의** — skill=모델이 호출하는 능력(SKILL.md) · mcp=외부 연결 표준 프로토콜(client–server, tools·resources·prompts, "AI용 USB-C") · plugin=확장 배포 패키지(marketplace) |
| 9 | 동작 원리 + 관계 | 각 구성요소 **동작 2스텝** (skill=progressive disclosure 로드 / mcp=연결·tool 호출 / plugin=마켓플레이스·네임스페이스) + **관계**(plugin=꾸러미, skill·mcp=담기는 능력·연결, 단독으로도 씀). ※ 레고 비유 폐기 |
| 10 | **Ex2 예시** | 미리 만든 *문서 parsing* 예시 — skill이 문서를 이렇게 읽고 구조화한다 (기본 동작) |
| 11 | 유용한 것들 소개 | **1장** — 3칸(Context확장 / Web / Token절약) × 각 대표 1개. 카테고리 지도만 압축 전달, 전체 목록은 부록 B |
| 12 | 설치 방법 + 주의 | 설치법, 보안 주의사항 간단히 |

## 본문 3 — harness (약 13분)

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 13 | harness란? | 모델을 감싸는 실행 틀(순서+검증+재시도). **청중 언어로 재구성 — DUT를 감싸는 verification harness(UVM testbench)와 같은 결**, 대상이 모델일 뿐 |
| 14 | 작동 방식 | 입력→skill 실행→스키마 검증→통과?(출력)/실패(재시도). 스키마 = **assertion·checker처럼 게이트** |
| 15 | 장점과 단점 | 얻는 것(일관 품질·재현성) vs 치르는 것(초기 설계·유지보수), 균형 있게 |
| 16 | **Ex4: 유무 성능차** ⭐ | *같은 parsing 작업*을 harness 없이 vs 있이 — 클라이맥스 |

## 마무리 (약 10분) — "변화 재강조 → 안전한 첫걸음" (순서 결합)

> 두 방향(변화 재강조 / 액션)을 택일하지 않고 순서로 결합. 정점으로 데우고 → 사내 안전한 한 걸음으로 착지.

| # | 페이지 | 담는 내용 |
|---|--------|-----------|
| 17 | 다시 그 장면 (정서 정점) | 오프닝 Ex1을 **정지컷 1장**으로 되짚음(재생 X). "이제 원리가 읽히죠?" — 경탄→자신감. Ex1은 마무리 통틀어 여기서 딱 1회만 소환 |
| 18 | 그래서 월요일에 뭐부터? (핵심 CTA) | 딱 하나 집어줌 — 사내 LLM에서 · 설계/검증 맥락 작은 작업(스펙 구조화·로그 요약) · **사람이 검수하는 루프** 유지 · "애매하면 보안 먼저". 사외·harness는 '도입되면'으로 시제 분리 |
| 19 | 질문 받습니다 (+ Q&A) | 강제 공유 없음. 지지대(부록 B·옆자리 동료) 한 줄 → 마지막 한 마디로 '이해→이번 주 행동' 밀어냄 → Q&A |

> **마지막 한 마디(안):** "바뀐 건 도구가 아니라, 그 장면을 읽어내는 여러분입니다 — 이번 주, 사내에서 딱 하나만 켜서 그 첫 대화를 시작해 보세요."
>
> ※ 정점 15초 길이·참여 요소 유무 등 세부는 슬라이드 제작 단계에서 조정.

---

## 페이지 수 요약

- 도입 5 + 본문1 2 + 본문2 5 + 본문3 4 + 마무리 3 = **약 19장**
- 데모 3개가 시간을 크게 먹으므로 페이지 수 대비 시간은 넉넉하지 않음
- 마무리 3장은 10분+Q&A 안에 들어가야 하므로 각 장을 가볍게 유지(특히 정점은 짧게)

## 이 리스트에서 아직 열린 것

- (개념·골격 단계 결정은 일단락) 다음은 슬라이드 제작 단계 세부 — 정점 길이, 참여 요소, 시각 표현
- 슬라이드 레이아웃: 본문 슬라이드를 **상단 정렬(top-anchor)** 로 바꿈(하단 여백 정리). 표지·마무리·정점은 중앙 정렬 유지. → 중앙 정렬 선호 시 한 줄로 되돌림 가능(taste)

## v0.4 콘텐츠 전문화 패스 (2026-07-06)

> 지적: 용어가 캐주얼·부정확, 기초조사 부실. → 5갈래 조사 + 각 검증(공식 문서 대조)로 보강 후 슬라이드 반영.

- **역사(2장)**: 4칸 뭉뚱 → 6개 연도·명칭 마일스톤(검증됨).
- **정의(8장)/동작(9장)**: 레고·부품상자·작업대 비유 폐기 → skill/mcp/plugin의 정확한 정의 + 동작 2스텝 + 관계(세 축이 다름: 배포 / 능력 / 연결 프로토콜).
- **harness(13~15장)**: "믿고 쓰는 자동화" 등 정서어 → "재현·검증되는 실행". 청중이 아는 **verification harness(UVM)** 에 빗대는 재구성 채택.
- 용어 교체: 표준 커넥터→표준 연결 프로토콜, 전문 기술→모델이 호출하는 능력 등.
- 성능 수치(속도·%)는 여전히 쓰지 않음(품질·재현성으로만) — 기존 방침 유지.

---

## 부록 A — Ex2 / Ex4 공유 parsing 예시 (실제 내용)

> 하나의 "문서 parsing" 작업을 Ex2·Ex4가 공유.
> 아래는 **구성된 예시(illustrative)** — 실제로 참인 메커니즘(스키마 검증 + 재시도가 일관성·품질을 높임)을
> 보여주기 위한 대표 사례. 특정 시스템의 측정값이 아님.
> 원하면 사용자가 자신의 harness 실제 실행 결과로 교체 가능(선택).

### 공유 입력 문서 (회의 메모)

```
[회의 메모]
10/24 주간 동기화. 참석: 지훈, 민아, 세영.
- 로그인 API 응답 지연 이슈, 원인은 토큰 갱신 로직으로 확인. 지훈이 금주 내 수정.
- 신규 대시보드 디자인 시안 3안 중 2안으로 결정. 민아가 다음 주 월요일까지 반영.
- 다음 회의 10/31 오후 3시.
```

- 중립적·누구나 이해 가능. 설계/검증 특화(UVM·RTL) 아님 → 범용 소개에 적합. (교체 가능)

### 목표 스키마

```
{ title, date, participants[], decisions[], action_items[{owner, task, due}], next_meeting }
```

### Ex2 — 기본 동작 (본문 2, 페이지 10)

비정형 문서를 skill이 한 번에 구조화. **잘 되는 케이스만** 보여줌.

```json
{
  "title": "주간 동기화",
  "date": "2024-10-24",
  "participants": ["지훈", "민아", "세영"],
  "decisions": ["대시보드 디자인 2안으로 결정"],
  "action_items": [
    { "owner": "지훈", "task": "로그인 API 토큰 갱신 로직 수정", "due": "금주" },
    { "owner": "민아", "task": "대시보드 2안 반영", "due": "2024-10-28" }
  ],
  "next_meeting": "2024-10-31 15:00"
}
```

- 가르치는 점: **비정형 in → 구조화 out**, skill이 하는 일을 눈으로 확인.

### Ex4 — 같은 작업, harness 유무 (본문 3, 페이지 16)

같은 입력을 **3번씩** 돌린다고 상상. 축은 **품질 + 재현성**.

**harness 없이 (단발 호출, 매번 결과가 흔들림)**

- Run 1 — 필드 누락 + 구조 드리프트: `participants`가 배열이 아니라 한 문자열, `action_items`에 `due` 빠짐
  ```json
  { "title": "주간 동기화", "participants": "지훈, 민아, 세영",
    "action_items": ["로그인 API 수정", "대시보드 반영"] }
  ```
- Run 2 — 키 이름이 바뀜(스키마 불일치): 한글 키로 나옴
  ```json
  { "제목": "주간 동기화", "참석자": ["지훈","민아","세영"], "결정": ["2안 채택"] }
  ```
- Run 3 — JSON 깨짐(기계 파싱 불가): 설명 문장이 앞에 붙고 마지막 쉼표 오류
  ```
  다음은 회의 내용을 정리한 결과입니다:
  { "title": "주간 동기화", "date": "2024-10-24", }
  ```

→ **품질**: 누락·형식 오류 발생. **재현성**: 세 번 모두 형태가 다름 → 뒷단에서 받아쓸 수 없음.

**harness로 감싸면 (스키마 검증 + 통과할 때까지 재시도)**

- Run 1·2·3 — **매번 동일**하게 Ex2의 스키마-일치 JSON 출력.
  ```json
  { "title": "...", "date": "2024-10-24", "participants": ["지훈","민아","세영"],
    "decisions": [...], "action_items": [{...}], "next_meeting": "2024-10-31 15:00" }
  ```

→ **품질**: 스키마 게이트를 통과한 것만 나오므로 항상 완결·유효. **재현성**: 세 번 모두 동일.

### 이 대조가 정직한 이유

- LLM 단발 호출은 확률적이라 실제로 키 이름·구조가 흔들리고 JSON이 깨지는 일이 흔함 — 지어낸 결함이 아님.
- harness의 "스키마 정의 → 검증 → 실패 시 재시도"는 출력을 스키마에 수렴시키는 실제 기법.
- 그래서 위 예시는 **참인 현상을 대표 사례로 보여주는 것**. 단, 특정 harness의 측정 수치인 척은 하지 않음.
- 속도/퍼센트 같은 성능 숫자는 쓰지 않음 — 품질·재현성으로만 승부.

---

## 부록 B — 유용한 것들 전체 목록 (페이지 11 뒤, 자가학습용)

> 발표 땐 넘기지 않음 (시간 비용 0). 자료를 받아본 사람이 세미나 후 스스로 탐색하는 진입로.
> 페이지 11은 아래 3칸 중 **각 대표 1개씩만** 본문에 노출하고, 나머지는 여기로.
> 대표 선정·설치 한 줄은 슬라이드 제작 단계에서 확정 (지금은 후보 배치까지만).

| 카테고리 | 후보 | 본문 대표(11번) |
|----------|------|------------------|
| Context 확장 | ralph loop · claude-mem · superpowers | (대표 1개 · 미정) |
| Web 개발 | firecrawl · frontend design · playwright | (대표 1개 · 미정) |
| Token 절약 | caveman · ponytail · yagni | (대표 1개 · 미정) |

- 설치 방법은 페이지 12(설치+주의)에서 공통으로 한 번, 개별 항목은 부록에 한 줄씩.
- 후보는 시놉 v0.5 기준. 실제 사내/사외 환경에서 검증되면 목록 조정 가능.
